# Claude working notes (per project convention)
Claude/

# Instagram session — contains auth tokens, NEVER commit
session.json

# Per-account download memory (media ids only, still local data)
cache/

# Downloaded media / resume state (in case downloads land inside the repo)
# (never name a downloads subfolder "instastash" — macOS git matches case-insensitively)
downloads/
.instastash_state.json
*.part

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local task notes
Tasks.md
